fullnameOverride: ""
backend:
  log_level: "info"
  image:
    name: image-registry.openshift-image-registry.svc:5000/coffee-app-build/backend:2024-10-23-16-18-1f4bf2035f01ec5ba090eb98b5f6d7255de56c9d
  ingress:
    host: api.test.beanscore.de
frontend:
  image:
    name: image-registry.openshift-image-registry.svc:5000/coffee-app-build/frontend:2024-10-24-06-36-2795007078572130b008c1622e6352fc710f62d1
  ingress:
    host: test.beanscore.de
resizer:
  logLevel: "info"
  image:
    name: image-registry.openshift-image-registry.svc:5000/coffee-app-build/resizer:2024-10-13-13-36-63f4e96cccaaba4d0384a760876640812689f925
mongodb:
  auth:
    existingSecret: mongodb-root-password
minio:
  auth:
    existingSecret: minio-credentials
keycloak:
  ingress:
    hostname: auth.test.beanscore.de
    annotations:
      cert-manager.io/cluster-issuer: letsencrypt
    extraTls:
      - hosts:
          # Make sure its same as ingress.hostname
          - auth.test.beanscore.de
        secretName: coffee-app-test-keycloak-tls
  postgresql:
    auth:
      existingSecret: keycloak-postgres-password
  auth:
    adminUser: admin
    existingSecret: keycloak-admin-password
