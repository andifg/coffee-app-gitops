fullnameOverride: ""
backend:
  log_level: "info"
  image:
    name: image-registry.openshift-image-registry.svc:5000/coffee-app-build/backend:2024-06-29-02-03-96de07e5f440eee3cf5633cbe2740cd3914ffa68
  ingress:
    host: api.test.beanscore.de
frontend:
  image:
    name: image-registry.openshift-image-registry.svc:5000/coffee-app-build/frontend:2024-06-20-02-03-e0839971f0d8e7308d4cde6a56ea4606f34c7bc3
  ingress:
    host: test.beanscore.de
resizer:
  logLevel: "info"
  image:
    name: image-registry.openshift-image-registry.svc:5000/coffee-app-build/resizer:2024-06-28-06-38-50ce2ae57aa2dc825df0b27e921b2bb1401b6a72
mongodb:
  auth:
    existingSecret: mongodb-root-password
minio:
  auth:
    existingSecret: minio-credentials
keycloak:
  ingress:
    hostname: auth.test.beanscore.de
    annotations:
      cert-manager.io/cluster-issuer: letsencrypt
    extraTls:
      - hosts:
          # Make sure its same as ingress.hostname
          - auth.test.beanscore.de
        secretName: coffee-app-test-keycloak-tls
  postgresql:
    auth:
      existingSecret: keycloak-postgres-password
  auth:
    adminUser: admin
    existingSecret: keycloak-admin-password
